package app.ssm.duck.duckapp.interfaces;

import android.view.View;

/**
 * Interface for receiving click events from cells.
 */
public interface OnItemClickListener {
    public void onClick(View view, int position);
}
